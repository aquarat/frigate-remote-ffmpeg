# Build outputs
dist/

# Root-level compiled binary (produced by ad-hoc `go build` invocations)
wrapper

# Local coordinator config (contains host-specific paths — use coordinator.yaml.example as a template)
config/coordinator.yaml
launchd/com.frigate.coordinator.plist
